import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const usemarketingGetters = () => {
  // 这里可以定义模块的 getters
  // 示例:
  // const getTotalItems = computed(() => items.value.length);
  // return { getTotalItems };
};
